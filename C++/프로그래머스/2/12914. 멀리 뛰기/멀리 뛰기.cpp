// 챗봇의 `Best Practice`
/*
작성해주신 코드를 보니 동적 계획법(DP, Dynamic Programming)의 핵심 원리를 완벽하게 파악하셨네요! 점화식을 정확히 세우셨고, 문제에서 요구하는 모듈러 연산(`% 1234567`)도 오버플로우가 발생하지 않게 반복문 내에 올바르게 적용하셨습니다. 정말 훌륭하게 작성된 코드입니다!

현재 코드도 충분히 정답으로 통과할 수 있지만, 메모리 사용량을 더욱 줄이는 **Best Practice(최적화된 방식)**가 존재합니다. 어떤 부분을 개선할 수 있는지, 그리고 어떻게 코드를 구현하면 되는지 단계별로 자세히 설명해 드릴게요.

---

### 💡 1. 솔루션 개요: 현재 코드의 개선 사항

* **현재 상태 (공간 복잡도 `O(N)`):** 지금은 크기가 `n + 1`인 `vector`를 만들어서 1부터 `n`까지의 모든 결과값을 메모리에 저장하고 있습니다. `n`이 매우 커진다면 그만큼 메모리 사용량도 늘어나게 됩니다.
* **개선 아이디어 (공간 복잡도 `O(1)`):** 점화식 `dp[i] = dp[i - 1] + dp[i - 2]`를 자세히 살펴보면, 현재 위치의 값을 계산하기 위해서는 **바로 직전 값**과 **그 이전 값**, 딱 2개의 데이터만 필요합니다. 즉, 전체 배열을 다 기억할 필요 없이 변수 2개만 갱신하며 재사용하면 메모리를 획기적으로 줄일 수 있습니다.

---
💻 2. Best Practice 코드 및 구현 지침
아래는 배열 대신 단 3개의 변수만을 사용하여 메모리 사용량을 최소화한 최적화 코드입니다. 프로그래머스에 바로 복사하여 붙여넣기 하실 수 있습니다.
*/
#include <string>
#include <vector>

using namespace std;

long long solution(int n) {
    // 1. 예외 및 초기 조건 처리
    if (n == 1) return 1;
    if (n == 2) return 2;
    
    // 2. 변수 초기화 (배열 대신 변수 사용)
    long long prev2 = 1;     // 이전의 이전 값 (i-2)
    long long prev1 = 2;     // 바로 이전 값 (i-1)
    long long current = 0;   // 현재 계산할 값 (i)
    
    // 3. 반복문을 통한 값 갱신 (상향식 DP)
    for (int i = 3; i <= n; ++i) {
        // 현재 값은 이전 두 값의 합에 모듈러 연산을 적용
        current = (prev1 + prev2) % 1234567;
        
        // 다음 반복을 위해 변수들을 한 칸씩 앞으로 이동(Shift)
        prev2 = prev1; 
        prev1 = current;
    }
    
    // 4. 최종 계산된 값 반환
    return current;
}

/*
### 📖 3. 코드 상세 문서 및 추론 과정

이 코드가 어떻게 동작하는지 단계별로 가르쳐 드릴게요.

* **`prev2`와 `prev1`의 역할:** 이 두 변수는 우리가 계단을 오를 때 딛고 있는 바로 아래의 두 칸이라고 생각하시면 됩니다. `i`가 3일 때, `prev2`는 1칸을 뛰는 방법의 수(1), `prev1`은 2칸을 뛰는 방법의 수(2)를 담고 시작합니다.
* **값의 이동 (Shift):**
계산을 마친 후 `prev2 = prev1;` 그리고 `prev1 = current;`를 수행합니다. 이는 다음 칸(`i+1`)을 계산하기 위해 우리가 바라보는 시점을 한 칸씩 앞으로 당기는 작업입니다. 오래된 데이터인 `prev2`는 버려지고, 가장 최근에 계산된 `current`가 새로운 `prev1`이 되는 것이죠.
* **제한사항 및 가정:** 문제에서 `n`은 1 이상 2000 이하의 정수라고 가정합니다. 변수를 `long long` 타입으로 선언하여 연산 과정에서 발생할 수 있는 자료형 오버플로우를 안전하게 방지했습니다.
*/