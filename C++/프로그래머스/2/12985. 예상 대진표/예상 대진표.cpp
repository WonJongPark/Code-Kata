// 챗 봇의 개선 사항

#include <iostream>

using namespace std;

int solution(int n, int a, int b)
{
    int round = 0;

    // A와 B의 번호가 같아질 때까지 (즉, 서로 대결할 때까지) 반복합니다.
    while (a != b) {
        // 정수 나눗셈을 활용하여 다음 라운드의 번호를 계산합니다.
        a = (a + 1) / 2;
        b = (b + 1) / 2;
        
        // 라운드를 1 증가시킵니다.
        round++;
    }

    return round;
}

/*
### 💡 개선 방안 (코드 최적화 아이디어)

**1. 짝수/홀수 조건문 단순화 (정수 나눗셈 활용)**
현재 짝수일 때는 `a / 2`, 홀수일 때는 `a / 2 + 1`을 하도록 나누어 두셨죠. C++의 **정수 나눗셈(Integer Division)** 특성을 활용하면 이를 한 줄로 줄일 수 있습니다.

* `(a + 1) / 2` 수식을 사용해 보세요.
* `a`가 1(홀수)일 때: `(1 + 1) / 2 = 1`
* `a`가 2(짝수)일 때: `(2 + 1) / 2 = 1`
* 짝수와 홀수 조건 없이 한 번에 다음 라운드 번호를 구할 수 있습니다.

**2. `while` 문을 활용한 반복문 변경**
현재 `<cmath>` 라이브러리의 `log2`를 이용해 최대 라운드 수를 구한 뒤 `for`문을 돌리고 있습니다. 하지만 우리가 찾아야 하는 종료 조건은 명확합니다. **"A와 B의 번호가 같아질 때"**죠. 따라서 굳이 전체 라운드 수를 계산할 필요 없이 `while (a != b)`를 사용하면 코드가 훨씬 직관적이게 되고 불필요한 라이브러리 포함도 줄일 수 있습니다.

---

### 📖 구현 지침 및 문서

* **제한사항 및 가정:** 이 코드는 이 주어지지만, 사실상 의 크기와 무관하게 와 의 번호만으로 추적하므로 $O(log N)$의 매우 빠른 시간 복잡도를 가집니다.
* **`while (a != b)`:** 두 참가자가 만나서 대결하게 되면, 다음 라운드로 올라갈 때 같은 번호를 부여받게 됩니다. 따라서 두 번호가 같지 않을 때까지만 루프를 실행하는 것이 가장 효율적입니다.
* **변수 설명:** 기존의 `answer` 대신 변수명을 `round`로 변경하여, 이 변수가 현재 몇 번째 라운드인지를 추적하고 있다는 것을 더 명확하게 표현했습니다.
*/